<section class="card">
  <header >{{post.name}} Posted by {{post.user.username}}</header>
  
   <p> {{post.body}}</p>

  </section>

    {{#each post.comments as |comment|}}
    {{> comment-details}}
    {{/each}}
